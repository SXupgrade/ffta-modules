# Copilot / coding agent instructions

Follow `AGENTS.md` first.

Keep the repository standalone and installable at:

```txt
Modules/Custom/ffta-modules
```

Do not import Compet+ private code.
Do not duplicate Ianseo database credentials.
Reuse Ianseo configuration and database access.
