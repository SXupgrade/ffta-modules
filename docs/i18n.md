# i18n

- Default identifiers, variable names and comments are English.
- UI labels must use i18n keys.
- English lives in `en.json`.
- French lives in `fr.json`.
- Modules call `app.t('namespace.key')`.
