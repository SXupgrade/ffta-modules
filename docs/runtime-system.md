# Runtime system

Initial supported runtime:

```txt
ianseo
```

The runtime is responsible for exposing adapters to the module context.

Future compatibility targets can implement the same contracts outside this repository:

```txt
competplus-web
competplus-desktop
```
