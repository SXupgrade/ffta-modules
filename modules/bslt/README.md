# Saisie Beursault

Legacy Ianseo module imported from `IanseoModules-main/Modules/Custom/Bslt` and wrapped as an ffta-modules module.

The original code is kept under `legacy/`. The wrapper provides `module.manifest.js`, `module.mount.js`, routes and i18n so the module can be discovered by the module loader.
